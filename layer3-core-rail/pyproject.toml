[project]
name = "layer3-core-rail"
version = "1.0.0"
description = "Deterministic TrustLlama pricing, directional burn, and anti-whale rules."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["layer3_core_rail*"]